{
  "name": "seaport",
  "version": "0.4.0",
  "private": true,
  "packageManager": "bun@1.3.13",
  "scripts": {
    "benchmark": "bun benchmarks/measure.ts",
    "release:prepare": "release-it --ci",
    "release:dry-run": "release-it --dry-run --ci",
    "release:metadata": "bun .github/scripts/release-metadata.ts"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^11.0.1",
    "release-it": "^20.0.0"
  }
}
