name: benchmark

on:
  workflow_dispatch:
    inputs:
      dataset:
        description: Dataset to benchmark with `run -d <dataset>`.
        required: true
        default: factory-ai/legacy-bench
      iterations:
        description: Number of runs per tool.
        required: true
        default: "10"
      extra_args:
        description: Optional extra arguments appended after `run -d <dataset>`.
        required: false
        default: ""

permissions:
  contents: read

jobs:
  seaport:
    name: benchmark seaport
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - name: Build Seaport
        run: cargo build --release --locked

      - name: Check Seaport
        run: ./target/release/seaport --help

      - name: Run benchmark
        env:
          BENCH_DATASET: ${{ inputs.dataset }}
          BENCH_EXTRA_ARGS: ${{ inputs.extra_args }}
          SEAPORT_BENCH_ITERATIONS: ${{ inputs.iterations }}
          SEAPORT_BENCH_OUTPUT_DIR: benchmarks/results/github-action/seaport
        run: |
          set -euo pipefail
          args=(run -d "$BENCH_DATASET")

          if [[ -n "$BENCH_EXTRA_ARGS" ]]; then
            # shellcheck disable=SC2206
            extra_args=( $BENCH_EXTRA_ARGS )
            args+=("${extra_args[@]}")
          fi

          ./benchmarks/benchmark.sh seaport "${args[@]}"

      - name: Upload benchmark logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-seaport
          path: benchmarks/results/github-action/seaport/
          if-no-files-found: ignore

  harbor:
    name: benchmark harbor
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - uses: actions/checkout@v6

      - name: Install Harbor
        run: |
          set -euo pipefail
          python3 -m pip install --user --upgrade pip
          python3 -m pip install --user harbor
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Check Harbor
        run: harbor --help

      - name: Run benchmark
        env:
          BENCH_DATASET: ${{ inputs.dataset }}
          BENCH_EXTRA_ARGS: ${{ inputs.extra_args }}
          SEAPORT_BENCH_ITERATIONS: ${{ inputs.iterations }}
          SEAPORT_BENCH_OUTPUT_DIR: benchmarks/results/github-action/harbor
        run: |
          set -euo pipefail
          args=(run -d "$BENCH_DATASET")

          if [[ -n "$BENCH_EXTRA_ARGS" ]]; then
            # shellcheck disable=SC2206
            extra_args=( $BENCH_EXTRA_ARGS )
            args+=("${extra_args[@]}")
          fi

          ./benchmarks/benchmark.sh harbor "${args[@]}"

      - name: Upload benchmark logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-harbor
          path: benchmarks/results/github-action/harbor/
          if-no-files-found: ignore
