[package]
name = "runtime"
version = "0.1.0"
edition = "2021"
description = "Rust-native agent evaluation platform with deterministic execution."
license = "MIT"

[lib]
name = "runtime"
path = "src/lib.rs"

[[bench]]
name = "evaluation"
harness = false
