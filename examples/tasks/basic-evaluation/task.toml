schema_version = "1.0"

[task]
name = "seaport/basic-evaluation"
description = "Minimal Seaport task that asks the agent to create a greeting file."

[agent]
timeout_sec = 120.0
user = "agent"

[verifier]
timeout_sec = 120.0

[environment]
docker_image = "ubuntu:24.04"
network_mode = "no-network"
